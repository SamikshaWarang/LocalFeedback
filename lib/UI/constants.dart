import 'package:flutter/material.dart';

const slider1 = BoxDecoration(
  image:
      DecorationImage(image: AssetImage('HomeSlider1.jpg'), fit: BoxFit.cover,colorFilter: ColorFilter.mode(Colors.grey,BlendMode.darken)),
);

const slider2 = BoxDecoration(
  image:
      DecorationImage(image: AssetImage('HomeSlider2.jpg'), fit: BoxFit.cover,colorFilter: ColorFilter.mode(Colors.grey, BlendMode.darken)),
);

const slider3 = BoxDecoration(
  image:
      DecorationImage(image: AssetImage('HomeSlider3.jpg'), fit: BoxFit.cover,colorFilter: ColorFilter.mode(Colors.grey, BlendMode.darken)),
);

const boxdeco1 = BoxDecoration(
    // borderRadius: BorderRadius.only(
    //     bottomLeft: Radius.circular(30),
    //     bottomRight: Radius.circular(30),
    //     topLeft: Radius.circular(30),
    //     topRight: Radius.circular(30)),
    shape: BoxShape.circle);

// String language;
